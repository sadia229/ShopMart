class Urls {
  static const baseUrl = "https://desktopit.org/dishnet/api";
  static const addToCartUrl = baseUrl + "/cart/add";
  static const loginUrl = baseUrl + "/login";
  static const profileUrl = baseUrl + "/profile";
  static const billUrl = baseUrl + "/responsible/area";
}
